pseudo classes: define a special state of an element
     -syntax: selector:pseudo-class {
  property: value;
}
Grid : used to display elements in row and column, where we can fix the column width 
     -Grid container: the parent (container) <div> element to display grid.
Animation in css: Animation property in css will be: Animation: name duration
@keyframe: name{
     transform
}
Animation libraries: 
animate css
aos libraries
locomotive scroll
   - webhosting: webserver
      -netlify (need index.html)
      -github pages
      - vercel

  ...................................................
                  git & github
 .....................................................

 GIT : used to control a version of a code 
      GIT commands: 
          -git status (used to check status of git inside the folder)
          -git init (used to create or initialize git repositiry in a folder)
          -git add . (used to track all untracked files to git)                      
          -git commit -m "message" (used to store/save tracked files to git)